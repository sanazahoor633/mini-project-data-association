<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- <link rel="stylesheet" href="/stylesheets/style.css"> -->
    <title>Document</title>
</head>
<body>
<div class="w-full min-h-screen bg-zinc-900 text-white p-8">
  <div class="w-full  flex justify-end">
     <a class="px-3 py-2 bg-red-500 text-white rounded-md inline-block" href="/logout">Logout</a>
  </div>
 
    
  <h5>Edit your post</h5>
  <form action="/update/<%= post._id %>" method="post">
    <textarea name="content" placeholder="whats on your mind " class="block p-2 w-1/2 resize-none outline-none bg-transparent border-2 border-zinc-500 rounded-md mt-4"><%= post.content %></textarea>
    <input class="bg-yellow-500 w-40 rounded px-4 py-2 block mt-4" type="submit" value="Update post">
  </form>
 
</div>
</body>
</html>